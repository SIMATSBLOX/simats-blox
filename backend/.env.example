# Used by server/index.js (set in repo root .env). Data: SQLite at server/data/ide.sqlite.

PORT=3847
CLIENT_ORIGIN=http://localhost:5173

# Must match JWT in server/index.js and backend/src/config/jwt.js (Socket.IO auth).
# JWT_SECRET=your-long-random-secret
